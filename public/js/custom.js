document.getElementById('profileButton').addEventListener('click', function() {
    const profileBox = document.getElementById('profileBox');
    profileBox.classList.toggle('hidden');  // Toggle the visibility of the box
});
